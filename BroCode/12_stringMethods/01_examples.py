
name = input("Enter your full name: ")

# result = len(name)
result = name.find(" ") # will return the first occurence of a given character
result = name.rfind # r reverse because here we are finding the last occurence of a given character

print(result)